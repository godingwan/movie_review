<%= simple_form_for([@movie, @review]) do |f| %>
  <%= f.input :score, required: true, label: 'Movie Score' %>
  <%= f.input :body, required: true, label: 'Review' %>
  <%= f.button :submit %>
<% end %>
